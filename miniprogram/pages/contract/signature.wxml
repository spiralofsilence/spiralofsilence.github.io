<view class="page">
  <view class="card">
    <view class="tab-bar">
      <view class="tab {{tabIndex === 0 ? 'active' : ''}}" data-index="0" bindtap="switchTab">手写签名</view>
      <view class="tab {{tabIndex === 1 ? 'active' : ''}}" data-index="1" bindtap="switchTab">数字签名</view>
    </view>
    <view wx:if="{{tabIndex === 0}}" class="canvas-wrap">
      <canvas
        canvas-id="signCanvas"
        class="signature-canvas"
        bindtouchstart="startDraw"
        bindtouchmove="moveDraw"
      />
      <view class="row" style="margin-top: 12rpx;">
        <text class="muted">在框内签名</text>
        <button class="button-ghost" size="mini" bindtap="clearCanvas">清空</button>
      </view>
    </view>
    <view wx:else class="digital-box">
      <input class="input" placeholder="请输入签署姓名" value="{{signerName}}" bindinput="onNameInput" />
      <view class="checkbox-row">
        <checkbox-group bindchange="toggleDigitalAgreement">
          <label class="checkbox-label">
            <checkbox value="agree" checked="{{digitalAgreed}}" />
            同意使用数字证书签名
          </label>
        </checkbox-group>
      </view>
      <text class="muted" style="margin-top: 8rpx; display: block;">
        使用数字证书签名将生成PDF存档
      </text>
    </view>
    <view style="margin-top: 16rpx;">
      <button class="button-primary" bindtap="saveSignature">保存签名</button>
    </view>
  </view>
</view>
